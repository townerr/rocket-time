import React from 'react';

type TimeReportProps = {
    id: number | undefined;
}

const TimesheetReport = (user: TimeReportProps) => {
  return (
    <div>TimesheetReport</div>
  )
}

export default TimesheetReport